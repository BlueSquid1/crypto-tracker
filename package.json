{
  "name": "crypto-tracker",
  "version": "0.0.0",
  "scripts": {
    "clean": "rm -rf ./dist/api && rm -rf ./dist/public/ && rm -rf ./node_modules",
    "build": "npm install ; tsc -p ./src/server/tsconfig.api.json ; docker build --tag node-docker .",
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "cp:www": "mkdir -p ./dist/api && cp ./src/server/index.js ./dist/api/index.js",
    "database:drop-tables": "docker volume rm $(docker volume ls -q)",
    "dev": "npm run cp:www ; concurrently -k \"tsc -p ./src/server/tsconfig.api.json -w\" \"nodemon -r ./dist/api/index.js --watch\""
  },
  "private": true,
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "mongodb": "^4.3.0",
    "rxjs": "~7.4.0",
    "tslib": "^2.3.0",
    "zone.js": "~0.11.4"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.13",
    "@types/jasmine": "~3.10.0",
    "@types/mongodb": "^4.0.7",
    "@types/node": "^12.11.1",
    "typescript": "~4.5.2"
  }
}
